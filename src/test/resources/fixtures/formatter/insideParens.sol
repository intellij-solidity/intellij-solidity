contract A {
function b(
int a,
A a
) {
    callFunction(1,2,3);
    callFunction(
  1,
  2,
  3
    );

  callFunction(
  1,
  2);

  if (a == 1 && b == 2)
  callMe(1,2);
  if (
  a == 1
  ) {
  }
  // TODO: fix && inside if
  if (a == 1
  && b == 2) {
    doSomething(1,2);
    2;
  }
  while(1==2
  || 2 == 3) {
    doSomething(1,2);
  }
}
}
