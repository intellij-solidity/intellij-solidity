contract A {
    function goforit() {
        uint lenght = 1;
        uint256[] memory result = new uint[](lenght);
        address[] memory ads = new address[](5);
        bool[] memory boolean = new bool[](2);
        bytes memory fetchedTokenByteCode = du();
        string memory abcde = "hello";
    }
}
