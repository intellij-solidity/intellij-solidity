contract MyToken {
    function MyToken() {
        Proposal({name: proposalNames[i], voteCount: 0});
        Proposal({name: proposalNames, voteCount: 0, number: 1});
        Proposal({name: proposalNames});
    }
}
