contract A {
    function f(uint[] memory yy, uint[] calldata) {
        uint[] storage x;
        uint[] memory y;
    }

    function setup(address[] calldata _owners, uint256 _threshold, address to, bytes calldata data) external {
    }
}
